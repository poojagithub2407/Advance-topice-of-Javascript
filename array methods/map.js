const numbers = [1, 2, 3, 4];

const newArray=numbers.map((num)=>num*3);
console.log('new array',newArray);


const words = ['hello', 'world'];

const upperWords=words.map((word)=>word.toUpperCase());
console.log('upperCase word',upperWords);

